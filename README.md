This is a simple React + TypeScript + Vite dashboard for displaying IoT sensor readings, including the 7-in-1 measurements: soil moisture, temperature, EC, pH, nitrogen (N), phosphorus (P), and potassium (K).
To run it locally:

Install dependencies:
npm install

Start the development server:
npm run dev